import Link from 'next/link';
const matches = [{ id: 'match-1', title: 'Team Pride vs Tribe Warriors', date: '2026-08-17', status: 'not started' }, { id: 'match-2', title: 'Lagos Kings vs Abuja Titans', date: '2026-08-18', status: 'stats entered' }];
export default function FantasyGameweekPage({ params }: { params: { gameweekId: string } }) { return <div className="p-8"><div className="mb-5 text-xs uppercase tracking-widest text-fn-muted"><Link href="/admin/fantasy" className="text-fn-green">Fantasy League</Link> / Gameweeks / {params.gameweekId}</div><h1 className="font-display text-2xl font-black uppercase text-fn-text">Gameweek matches</h1><p className="mt-2 text-sm text-fn-muted">Select a match to enter or edit its per-athlete stats.</p><div className="mt-5 grid gap-3">{matches.map((match) => <Link key={match.id} href={`/admin/fantasy/gameweeks/${params.gameweekId}/matches/${match.id}`} className="flex flex-wrap items-center justify-between gap-3 border border-fn-gborder bg-fn-card p-4 hover:border-fn-green"><div><h2 className="font-black uppercase text-fn-text">{match.title}</h2><p className="text-xs text-fn-muted">{match.date} · PUBG Mobile</p></div><span className="border border-fn-green/30 bg-fn-green/10 px-3 py-2 text-[10px] font-black uppercase text-fn-green">{match.status}</span></Link>)}</div></div>; }
