import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Custom Wager Disputes' };
export default function AdminCustomWagersLayout({children}:{children:React.ReactNode}){return children;}
