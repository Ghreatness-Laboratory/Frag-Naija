import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'AI Support Chatbot' };
export default function AdminSupportLayout({children}:{children:React.ReactNode}){return children;}
